{
  "name": "@mindview/desktop",
  "private": true,
  "version": "0.1.0",
  "description": "Electron shell for MindView — window only. Boots the Node server as a child process and loads http://127.0.0.1:<port>/.",
  "author": "Café Labs",
  "main": "dist/main.cjs",
  "scripts": {
    "build": "node esbuild.mjs",
    "dev": "npm run build && electron .",
    "stage": "node scripts/stage.mjs && node scripts/fetch-node.mjs",
    "dist": "npm run build && npm run stage && electron-builder --publish never",
    "dist:dir": "npm run build && npm run stage && electron-builder --dir --publish never",
    "typecheck": "tsc"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "electron": "33.4.11",
    "electron-builder": "^25.1.8",
    "esbuild": "^0.28.2",
    "typescript": "^5.6.3"
  }
}
